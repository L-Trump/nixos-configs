From 1b130b57f43933d6e2f3aa0bee6165862076ca92 Mon Sep 17 00:00:00 2001
From: LTClaw <ltclaw@localhost>
Date: Fri, 24 Jul 2026 17:45:24 +0800
Subject: [PATCH] fix: stabilize deferred compaction debt handling

---
 src/compaction.ts               |   9 ++
 src/engine.ts                   |  79 ++++++----
 src/openclaw-bridge.ts          |   2 +
 test/engine-maintenance.test.ts | 258 +++++++++++++++++++++++++++++---
 test/spend-guard-policy.test.ts |  33 ++++
 5 files changed, 333 insertions(+), 48 deletions(-)

diff --git a/src/compaction.ts b/src/compaction.ts
index 1ffe542..d017ba9 100644
--- a/src/compaction.ts
+++ b/src/compaction.ts
@@ -51,6 +51,8 @@ export interface CompactionResult {
   authFailure?: boolean;
   /** Whether the sweep stopped because its iteration or wall-clock budget was exhausted */
   stoppedAtBudget?: boolean;
+  /** A policy/eligibility condition prevented a summarizer call. */
+  blockedReason?: "below-threshold" | "empty-context" | "no-eligible-candidates";
 }

 export interface CompactionConfig {
@@ -992,6 +994,7 @@ export class CompactionEngine {
         tokensBefore,
         tokensAfter: tokensBefore,
         condensed: false,
+        blockedReason: "below-threshold",
       };
     }

@@ -1002,6 +1005,7 @@ export class CompactionEngine {
         tokensBefore,
         tokensAfter: tokensBefore,
         condensed: false,
+        blockedReason: "empty-context",
       };
     }

@@ -1247,6 +1251,10 @@ export class CompactionEngine {
     }

     const tokensAfter = runningTokens;
+    const blockedReason =
+      !actionTaken && !hadAuthFailure && !stoppedAtBudget && !stoppedForNoProgress
+        ? "no-eligible-candidates"
+        : undefined;

     return {
       actionTaken,
@@ -1257,6 +1265,7 @@ export class CompactionEngine {
       level,
       ...(hadAuthFailure ? { authFailure: true } : {}),
       ...(stoppedAtBudget ? { stoppedAtBudget: true } : {}),
+      ...(blockedReason ? { blockedReason } : {}),
     };
   }

diff --git a/src/engine.ts b/src/engine.ts
index 1410340..1ffd649 100644
--- a/src/engine.ts
+++ b/src/engine.ts
@@ -120,6 +120,8 @@ const HOST_SESSION_END_REASON_DELETED = "deleted";
 const HOST_SESSION_END_REASON_RESET = "reset";
 const CONTEXT_ENGINE_PROJECTION_EPOCH_VERSION = "summary-prefix-v1";
 const DEFERRED_ASSEMBLY_DEGRADED_PRESSURE_RATIO = 0.75;
+const DEFERRED_ASSEMBLY_EMERGENCY_PRESSURE_RATIO = 1;
+const ASSEMBLE_FORCE_MAX_RETRY_ATTEMPTS = 3;
 type CompactionExecutionParams = {
   conversationId: number;
   sessionId: string;
@@ -780,7 +782,11 @@ export class LcmContextEngine implements ContextEngine {
     currentTokenCount?: number;
     runtimeContext?: ContextEngineMaintenanceRuntimeContext;
     legacyParams?: Record<string, unknown>;
-  }): Promise<(ContextEngineMaintenanceResult & { exhausted?: boolean }) | null> {
+    /** Assemble-only emergency bypass for retry and summary-spend backoff. */
+    force?: boolean;
+  }): Promise<
+    (ContextEngineMaintenanceResult & { exhausted?: boolean; blockedByPolicy?: boolean }) | null
+  > {
     const maintenance = await this.compactionMaintenanceStore.getConversationCompactionMaintenance(
       params.conversationId,
     );
@@ -794,12 +800,12 @@ export class LcmContextEngine implements ContextEngine {
       scope: this.resolveSessionQueueKey(params.sessionId, params.sessionKey),
     });

-    if (
-      maintenance.nextAttemptAfter !== null &&
-      maintenance.nextAttemptAfter.getTime() > Date.now()
-    ) {
+    const nextAttemptAfter = maintenance.nextAttemptAfter;
+    const backoffActive =
+      nextAttemptAfter !== null && nextAttemptAfter.getTime() > Date.now();
+    if (!params.force && backoffActive) {
       this.deps.log.debug(
-        `[lcm] maintain: deferred compaction backoff active conversation=${params.conversationId} ${sessionLabel} retryAttempts=${maintenance.retryAttempts} nextAttemptAfter=${maintenance.nextAttemptAfter.toISOString()} debtReason=${maintenance.reason ?? "null"}`,
+        `[lcm] maintain: deferred compaction backoff active conversation=${params.conversationId} ${sessionLabel} retryAttempts=${maintenance.retryAttempts} nextAttemptAfter=${nextAttemptAfter.toISOString()} debtReason=${maintenance.reason ?? "null"}`,
       );
       return {
         changed: false,
@@ -808,6 +814,11 @@ export class LcmContextEngine implements ContextEngine {
         reason: "deferred compaction backoff active",
       };
     }
+    if (params.force && backoffActive) {
+      this.deps.log.warn(
+        `[lcm] consumeDeferredCompactionDebt: emergency force skipping backoff conversation=${params.conversationId} ${sessionLabel} retryAttempts=${maintenance.retryAttempts} nextAttemptAfter=${nextAttemptAfter.toISOString()}`,
+      );
+    }

     await this.compactionMaintenanceStore.markProactiveCompactionRunning({
       conversationId: params.conversationId,
@@ -913,21 +924,20 @@ export class LcmContextEngine implements ContextEngine {
         contextThresholdOverride: resolvedContextThreshold,
         runtimeContext: params.runtimeContext,
         legacyParams: params.legacyParams,
+        force: params.force,
       });
       const blockedByAuthCircuitBreaker = result.reason === "circuit breaker open";
-      // #639 Mode 2: terminal compaction exhaustion (no eligible candidates while
-      // over target) is non-retryable — clear the debt instead of pinning it and
-      // climbing retry_attempts forever (which thrashes the assemble degraded
-      // fallback). executeCompactionCore still returns ok=false here, so overflow
-      // recovery keeps the honest signal; only the deferred-debt maintenance
-      // treats it as done.
+      const blockedByPolicy = result.blockedByPolicy === true;
+      // True terminal exhaustion clears debt. Eligibility-policy blocks are
+      // different: no summarizer call failed, and a later turn can move the
+      // fresh-tail or structural frontier. Keep those pending without a timer.
       const compactionExhausted =
-        (result as { exhausted?: boolean }).exhausted === true;
+        (result as { exhausted?: boolean }).exhausted === true && !blockedByPolicy;
       const keepPending =
-        (!result.ok || blockedByAuthCircuitBreaker) && !compactionExhausted;
+        blockedByPolicy || ((!result.ok || blockedByAuthCircuitBreaker) && !compactionExhausted);
       const failureSummary = blockedByAuthCircuitBreaker
         ? "summary provider circuit breaker is open"
-        : result.ok || compactionExhausted
+        : result.ok || compactionExhausted || blockedByPolicy
           ? null
           : result.reason ?? "deferred compaction failed";
       const summarySpendBackoffUntil = keepPending
@@ -949,6 +959,7 @@ export class LcmContextEngine implements ContextEngine {
         rewrittenEntries: 0,
         ...(result.reason ? { reason: result.reason } : {}),
         ...(compactionExhausted ? { exhausted: true } : {}),
+        ...(blockedByPolicy ? { blockedByPolicy: true } : {}),
       };
     } catch (error) {
       await this.compactionMaintenanceStore.markProactiveCompactionFinished({
@@ -1012,6 +1023,7 @@ export class LcmContextEngine implements ContextEngine {
                 ...(telemetry.model ? { model: telemetry.model } : {}),
               }
             : undefined;
+        const forceAllowed = maintenance.retryAttempts < ASSEMBLE_FORCE_MAX_RETRY_ATTEMPTS;
         const result = await this.consumeDeferredCompactionDebt({
           conversationId: params.conversationId,
           sessionId: params.sessionId,
@@ -1019,6 +1031,7 @@ export class LcmContextEngine implements ContextEngine {
           tokenBudget: cappedTokenBudget,
           currentTokenCount: normalizedCurrentTokenCount,
           legacyParams: deferredLegacyParams,
+          force: forceAllowed,
         });
         drainResult = { exhausted: result?.exhausted === true };
       },
@@ -1287,6 +1300,7 @@ export class LcmContextEngine implements ContextEngine {
       // attempt and punishing progress with a spend backoff.
       let chainedSweeps = 1;
       let lastRoundMadeProgress = sweepResult.actionTaken === true;
+      let lastRoundBlockedReason = sweepResult.blockedReason;
       const sweepChainDeadlineAt = startedAt + this.resolveSweepChainDeadlineMs();
       const maxChainedSweeps = resolvePositiveInteger(
         this.config.maxSweepIterations,
@@ -1321,6 +1335,7 @@ export class LcmContextEngine implements ContextEngine {
           next.actionTaken === true &&
           (previousTokensAfter === undefined ||
             (nextTokensAfter !== undefined && nextTokensAfter < previousTokensAfter));
+        lastRoundBlockedReason = next.blockedReason;
         sweepResult = {
           ...next,
           actionTaken: sweepResult.actionTaken || next.actionTaken,
@@ -1342,21 +1357,21 @@ export class LcmContextEngine implements ContextEngine {
       const isUnderTargetAfterSweep = isUnderTargetAfter(sweepResult);
       const thresholdSweepStillOverTarget =
         isThresholdSweep && sweepResult.actionTaken && !isUnderTargetAfterSweep;
+      const thresholdSweepPolicyBlockedOverTarget =
+        isThresholdSweep &&
+        lastRoundBlockedReason === "no-eligible-candidates" &&
+        !isUnderTargetAfterSweep;
       const thresholdSweepStoppedAtBudget =
         (sweepResult as { stoppedAtBudget?: boolean }).stoppedAtBudget === true;
-      // #639 Mode 2 (deferred-compaction wedge): a threshold sweep that took NO
-      // action and did NOT fail (no eligible leaf/condensed candidates remain)
-      // while still over target is TERMINAL EXHAUSTION. Compaction shrinks STORED
-      // leaves but cannot reduce the host's OBSERVED live tokens, so retrying the
-      // same sweep can never make progress. We keep ok=false below (so overflow
-      // recovery / #15 still see the honest still-over-target failure) but flag
-      // it so the deferred-debt drain treats it as non-retryable and clears the
-      // debt instead of pinning maintenance.pending + climbing retry_attempts.
+      // A no-action result is terminal only when it is not an eligibility-policy
+      // block. Policy blocks remain retryable when later messages move the
+      // fresh-tail or structural frontier, and must not consume spend backoff.
       const thresholdSweepExhaustedOverTarget =
         isThresholdSweep &&
         !sweepResult.actionTaken &&
         !sweepResult.authFailure &&
         !thresholdSweepStoppedAtBudget &&
+        !thresholdSweepPolicyBlockedOverTarget &&
         !isUnderTargetAfterSweep;
       // Transcript wedge (lossless-claw-30b.4): terminal exhaustion with an
       // explicit host-observed token count means stored compaction has
@@ -1376,6 +1391,8 @@ export class LcmContextEngine implements ContextEngine {
         ? (sweepResult.actionTaken
             ? "provider auth failure after partial compaction"
             : "provider auth failure")
+        : thresholdSweepPolicyBlockedOverTarget
+          ? "compaction deferred until more context is eligible"
         : thresholdSweepStillOverTarget
           ? "compacted but still over target"
         : sweepResult.actionTaken
@@ -1394,7 +1411,11 @@ export class LcmContextEngine implements ContextEngine {
       }
       let spendBackoffOpened = false;
       if (thresholdSweepStillOverTarget && !sweepResult.authFailure) {
-        if (lastRoundMadeProgress) {
+        if (thresholdSweepPolicyBlockedOverTarget) {
+          this.deps.log.info(
+            `[lcm] compact: spend backoff skipped conversation=${conversationId} ${sessionLabel} scope=${summarySpendScopeKey} reason=eligibility_policy blockedReason=${lastRoundBlockedReason} chainedSweeps=${chainedSweeps} tokensAfter=${sweepResult.tokensAfter}`,
+          );
+        } else if (lastRoundMadeProgress) {
           // The attempt ended at a deadline while still reducing tokens.
           // Progress is persisted; the deferred drain or next attempt
           // continues from here, so opening a backoff would only punish
@@ -1419,6 +1440,7 @@ export class LcmContextEngine implements ContextEngine {
         compacted: sweepResult.actionTaken,
         reason: sweepReason,
         ...(thresholdSweepExhaustedOverTarget ? { exhausted: true } : {}),
+        ...(thresholdSweepPolicyBlockedOverTarget ? { blockedByPolicy: true } : {}),
         result: {
           tokensBefore: decision.currentTokens,
           tokensAfter: sweepResult.tokensAfter,
@@ -3590,13 +3612,18 @@ export class LcmContextEngine implements ContextEngine {
         const pressureThreshold = Math.floor(
           tokenBudget * DEFERRED_ASSEMBLY_DEGRADED_PRESSURE_RATIO,
         );
+        const emergencyPressureThreshold = Math.floor(
+          tokenBudget * DEFERRED_ASSEMBLY_EMERGENCY_PRESSURE_RATIO,
+        );
         let pressure = resolveDeferredAssemblyPressure({
           liveContextTokens,
           maintenance,
         });
-        if (pressure.pressureTokenCount > tokenBudget) {
+        if (pressure.pressureTokenCount >= emergencyPressureThreshold) {
+          const emergencyReason =
+            pressure.pressureTokenCount > tokenBudget ? "over-budget" : "critical-pressure";
           this.deps.log.warn(
-            `[lcm] assemble: emergency deferred compaction debt draining pre-assembly conversation=${conversation.conversationId} ${sessionLabel} currentTokenCount=${pressure.observedContextTokens} projectedTokenCount=${pressure.projectedTokenCount ?? "null"} tokenBudget=${tokenBudget} reason=over-budget`,
+            `[lcm] assemble: emergency deferred compaction debt draining pre-assembly conversation=${conversation.conversationId} ${sessionLabel} currentTokenCount=${pressure.observedContextTokens} projectedTokenCount=${pressure.projectedTokenCount ?? "null"} tokenBudget=${tokenBudget} emergencyThreshold=${emergencyPressureThreshold} reason=${emergencyReason}`,
           );
           let emergencyDrainResult: { exhausted: boolean } | null = null;
           try {
diff --git a/src/openclaw-bridge.ts b/src/openclaw-bridge.ts
index 28ca4ee..f22a5f1 100644
--- a/src/openclaw-bridge.ts
+++ b/src/openclaw-bridge.ts
@@ -63,6 +63,8 @@ export type CompactResult = {
    * uses this to stop re-queuing the sweep.
    */
   exhausted?: boolean;
+  /** Compaction is temporarily blocked by eligibility policy, not summarizer failure. */
+  blockedByPolicy?: boolean;
 };

 export type ContextEngineMaintenanceResult = {
diff --git a/test/engine-maintenance.test.ts b/test/engine-maintenance.test.ts
index 2983354..4043a5a 100644
--- a/test/engine-maintenance.test.ts
+++ b/test/engine-maintenance.test.ts
@@ -814,7 +814,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       });

       expect(first.changed).toBe(true);
-      expect(first.reason).toBe("compacted but still over target");
+      expect(first.reason).toBe("compaction deferred until more context is eligible");
       expect(complete.mock.calls.length).toBeGreaterThan(0);
       expect(complete.mock.calls.length).toBeLessThanOrEqual(maxSweepIterations * 2);
       const calledProviders = new Set(
@@ -827,8 +827,9 @@ describe("LcmContextEngine maintain and assemble budget", () => {
         .getConversationCompactionMaintenance(conversation.conversationId);
       expect(maintenance?.pending).toBe(true);
       expect(maintenance?.running).toBe(false);
-      expect(maintenance?.lastFailureSummary).toBe("compacted but still over target");
-      expect(maintenance?.nextAttemptAfter?.toISOString()).toBe("2026-05-31T13:00:00.000Z");
+      expect(maintenance?.lastFailureSummary).toBeNull();
+      expect(maintenance?.retryAttempts).toBe(0);
+      expect(maintenance?.nextAttemptAfter).toBeNull();

       const afterFirstCallCount = complete.mock.calls.length;
       const second = await engine.maintain({
@@ -841,7 +842,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
         },
       });
       expect(second.changed).toBe(false);
-      expect(second.reason).toBe("deferred compaction backoff active");
+      expect(second.reason).toBe("compaction deferred until more context is eligible");
       expect(complete).toHaveBeenCalledTimes(afterFirstCallCount);

       const summaries = await summaryStore.getSummariesByConversation(conversation.conversationId);
@@ -976,7 +977,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     expect(assembleResult.messages).toHaveLength(1);
   });

-  it("assemble() uses bounded live context when pending maintenance is near budget", async () => {
+  it("assemble() uses bounded live context below the emergency compaction watermark", async () => {
     const log = {
       info: vi.fn(),
       warn: vi.fn(),
@@ -997,7 +998,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
         seq: 0,
         role: "user",
         content: "stored context should be skipped while maintenance is pending",
-        tokenCount: 20,
+        tokenCount: 78,
       },
     ]);
     await engine
@@ -1007,7 +1008,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       conversationId: conversation.conversationId,
       reason: "threshold",
       tokenBudget: 100,
-      currentTokenCount: 90,
+      currentTokenCount: 78,
     });
     const executeCompactionCoreSpy = vi.spyOn(privateEngine, "executeCompactionCore");

@@ -1026,12 +1027,62 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       "current delivery turn",
     ]);
     expect(assembleResult.estimatedTokens).toBeLessThanOrEqual(100);
+    expect(assembleResult).not.toHaveProperty("promptAuthority");
     expect(log.warn).toHaveBeenCalledWith(
       expect.stringContaining("[lcm] assemble: degraded live fallback"),
     );
     expect(log.warn).toHaveBeenCalledWith(expect.stringContaining("reason=near-budget"));
   });

+  it("assemble() starts forced emergency compaction at the 100 percent watermark", async () => {
+    const log = {
+      info: vi.fn(),
+      warn: vi.fn(),
+      error: vi.fn(),
+      debug: vi.fn(),
+    };
+    const engine = createEngineWithDepsOverrides({ log });
+    const privateEngine = engine as unknown as {
+      executeCompactionCore: (params: unknown) => Promise<unknown>;
+    };
+    const sessionId = "assemble-critical-watermark-forces-compaction";
+    const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
+      sessionKey: undefined,
+    });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored critical context",
+        tokenCount: 100,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
+    await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
+      conversationId: conversation.conversationId,
+      reason: "threshold",
+      tokenBudget: 100,
+      currentTokenCount: 100,
+    });
+    const executeSpy = vi.spyOn(privateEngine, "executeCompactionCore").mockResolvedValue({
+      ok: true,
+      compacted: true,
+      reason: "compacted",
+    });
+
+    await engine.assemble({
+      sessionId,
+      messages: [makeMessage({ role: "user", content: "current turn" })],
+      tokenBudget: 100,
+    });
+
+    expect(executeSpy).toHaveBeenCalledWith(expect.objectContaining({ force: true }));
+    expect(log.warn).toHaveBeenCalledWith(expect.stringContaining("reason=critical-pressure"));
+  });
+
   it("assemble() intercepts large tool results in live messages before degraded fallback", async () => {
     const largeFilesDir = mkdtempSync(join(tmpdir(), "lossless-claw-large-files-"));
     tempDirs.push(largeFilesDir);
@@ -1059,7 +1110,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
         seq: 0,
         role: "user",
         content: "stored content",
-        tokenCount: 20,
+        tokenCount: 78,
       },
     ]);
     await engine
@@ -1132,7 +1183,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     ).resolves.toHaveLength(1);
   });

-  it("assemble() clears exhausted threshold debt and preserves leading system context via degraded fallback (#639 Mode 2)", async () => {
+  it("assemble() preserves leading system context while eligibility-blocked debt stays pending", async () => {
     const log = {
       info: vi.fn(),
       warn: vi.fn(),
@@ -1144,6 +1195,18 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
       sessionKey: undefined,
     });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored protected context",
+        tokenCount: 30,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
     await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
       conversationId: conversation.conversationId,
       reason: "threshold",
@@ -1160,27 +1223,27 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       tokenBudget: 10,
     });

-    // #639 Mode 2: exhausted threshold debt (empty conversation -> nothing to
-    // compact) is now CLEARED rather than left pending. Because this drain
-    // happens during an already-over-budget assemble call, the current turn still
-    // uses the degraded fallback instead of returning raw live messages.
     expect(assembleResult.messages.map((message) => message.content)).toEqual([
       "critical runtime policy",
       "current delivery turn",
     ]);
+    expect(assembleResult).toHaveProperty(
+      "promptAuthority",
+      "preassembly_may_overflow",
+    );
     const maintenance = await engine
       .getCompactionMaintenanceStore()
       .getConversationCompactionMaintenance(conversation.conversationId);
-    expect(maintenance?.pending).toBe(false);
+    expect(maintenance?.pending).toBe(true);
     expect(log.warn).toHaveBeenCalledWith(
       expect.stringContaining("[lcm] assemble: degraded live fallback"),
     );
     expect(log.warn).toHaveBeenCalledWith(
-      expect.stringContaining("reason=emergency-debt-exhausted"),
+      expect.stringContaining("reason=emergency-debt-still-pending"),
     );
   });

-  it("assemble() bounds live context when emergency debt drain reaches exhaustion", async () => {
+  it("assemble() bounds live context while emergency debt remains eligibility-blocked", async () => {
     const log = {
       info: vi.fn(),
       warn: vi.fn(),
@@ -1192,6 +1255,18 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
       sessionKey: undefined,
     });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored oversized context",
+        tokenCount: 500,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
     await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
       conversationId: conversation.conversationId,
       reason: "threshold",
@@ -1211,7 +1286,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const maintenance = await engine
       .getCompactionMaintenanceStore()
       .getConversationCompactionMaintenance(conversation.conversationId);
-    expect(maintenance?.pending).toBe(false);
+    expect(maintenance?.pending).toBe(true);
     expect(assembleResult.messages.map((message) => message.content)).toEqual([
       "current delivery turn",
     ]);
@@ -1220,11 +1295,15 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     expect(assembleResult.estimatedTokens).toBe(
       estimateSerializedMessagesTokens(assembleResult.messages),
     );
+    expect(assembleResult).toHaveProperty(
+      "promptAuthority",
+      "preassembly_may_overflow",
+    );
     expect(log.warn).toHaveBeenCalledWith(
       expect.stringContaining("[lcm] assemble: degraded live fallback"),
     );
     expect(log.warn).toHaveBeenCalledWith(
-      expect.stringContaining("reason=emergency-debt-exhausted"),
+      expect.stringContaining("reason=emergency-debt-still-pending"),
     );
   });

@@ -1249,7 +1328,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
         seq: 0,
         role: "user",
         content: "stored context should not be used after failed emergency compaction",
-        tokenCount: 20,
+        tokenCount: 150,
       },
     ]);
     await engine
@@ -1292,6 +1371,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     expect(assembleResult.messages.map((message) => message.content)).toEqual([
       "current emergency turn",
     ]);
+    expect(assembleResult).not.toHaveProperty("promptAuthority");
     expect(log.warn).toHaveBeenCalledWith(
       expect.stringContaining(
         "[lcm] assemble: emergency deferred compaction debt draining pre-assembly",
@@ -1320,6 +1400,18 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
       sessionKey: undefined,
     });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored over-budget context",
+        tokenCount: 30,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
     await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
       conversationId: conversation.conversationId,
       reason: "threshold",
@@ -1517,6 +1609,18 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
       sessionKey: undefined,
     });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored queued emergency context",
+        tokenCount: 20,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
     await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
       conversationId: conversation.conversationId,
       reason: "threshold",
@@ -1555,7 +1659,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     expect(assembleResult.messages).toHaveLength(1);
   });

-  it("assemble() degrades instead of spending during active deferred retry backoff", async () => {
+  it("assemble() emergency drain bypasses active retry backoff with force", async () => {
     const engine = createEngine();
     const privateEngine = engine as unknown as {
       executeCompactionCore: (params: unknown) => Promise<unknown>;
@@ -1564,6 +1668,18 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
       sessionKey: undefined,
     });
+    const [storedMessage] = await engine.getConversationStore().createMessagesBulk([
+      {
+        conversationId: conversation.conversationId,
+        seq: 0,
+        role: "user",
+        content: "stored emergency context",
+        tokenCount: 20,
+      },
+    ]);
+    await engine
+      .getSummaryStore()
+      .appendContextMessages(conversation.conversationId, [storedMessage.messageId]);
     await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
       conversationId: conversation.conversationId,
       reason: "threshold",
@@ -1578,7 +1694,11 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       failureSummary: "provider timeout",
       keepPending: true,
     });
-    const executeSpy = vi.spyOn(privateEngine, "executeCompactionCore");
+    const executeSpy = vi.spyOn(privateEngine, "executeCompactionCore").mockResolvedValue({
+      ok: false,
+      compacted: false,
+      reason: "provider timeout",
+    });

     const assembleResult = await engine.assemble({
       sessionId,
@@ -1586,7 +1706,7 @@ describe("LcmContextEngine maintain and assemble budget", () => {
       tokenBudget: 10,
     });

-    expect(executeSpy).not.toHaveBeenCalled();
+    expect(executeSpy).toHaveBeenCalledWith(expect.objectContaining({ force: true }));
     expect(assembleResult.messages).toHaveLength(1);
     const maintenance = await engine
       .getCompactionMaintenanceStore()
@@ -1594,6 +1714,100 @@ describe("LcmContextEngine maintain and assemble budget", () => {
     expect(maintenance?.pending).toBe(true);
   });

+  it("assemble() limits forced backoff bypass to three failed attempts", async () => {
+    const engine = createEngine();
+    const sessionId = "assemble-emergency-force-retry-cap";
+    const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
+      sessionKey: undefined,
+    });
+    const store = engine.getCompactionMaintenanceStore();
+    await store.requestProactiveCompactionDebt({
+      conversationId: conversation.conversationId,
+      reason: "threshold",
+      tokenBudget: 100,
+      currentTokenCount: 80,
+    });
+    for (let index = 0; index < 2; index += 1) {
+      await store.markProactiveCompactionRunning({ conversationId: conversation.conversationId });
+      await store.markProactiveCompactionFinished({
+        conversationId: conversation.conversationId,
+        failureSummary: "provider timeout",
+        keepPending: true,
+      });
+    }
+
+    const privateEngine = engine as unknown as {
+      maybeConsumeDeferredCompactionDebtForAssemble: (params: {
+        conversationId: number;
+        sessionId: string;
+        tokenBudget: number;
+      }) => Promise<unknown>;
+      consumeDeferredCompactionDebt: (params: unknown) => Promise<unknown>;
+    };
+    const consumeSpy = vi.spyOn(privateEngine, "consumeDeferredCompactionDebt").mockResolvedValue(null);
+
+    await privateEngine.maybeConsumeDeferredCompactionDebtForAssemble({
+      conversationId: conversation.conversationId,
+      sessionId,
+      tokenBudget: 100,
+    });
+    expect(consumeSpy).toHaveBeenLastCalledWith(expect.objectContaining({ force: true }));
+
+    await store.markProactiveCompactionRunning({ conversationId: conversation.conversationId });
+    await store.markProactiveCompactionFinished({
+      conversationId: conversation.conversationId,
+      failureSummary: "provider timeout",
+      keepPending: true,
+    });
+    await privateEngine.maybeConsumeDeferredCompactionDebtForAssemble({
+      conversationId: conversation.conversationId,
+      sessionId,
+      tokenBudget: 100,
+    });
+    expect(consumeSpy).toHaveBeenLastCalledWith(expect.objectContaining({ force: false }));
+  });
+
+  it("keeps eligibility-blocked debt pending without retry backoff", async () => {
+    const engine = createEngine();
+    const privateEngine = engine as unknown as {
+      executeCompactionCore: (params: unknown) => Promise<unknown>;
+    };
+    const sessionId = "maintain-policy-blocked-no-backoff";
+    const conversation = await engine.getConversationStore().getOrCreateConversation(sessionId, {
+      sessionKey: undefined,
+    });
+    await engine.getCompactionMaintenanceStore().requestProactiveCompactionDebt({
+      conversationId: conversation.conversationId,
+      reason: "threshold",
+      tokenBudget: 4_096,
+      currentTokenCount: 3_500,
+    });
+    vi.spyOn(privateEngine, "executeCompactionCore").mockResolvedValue({
+      ok: false,
+      compacted: true,
+      reason: "compaction deferred until more context is eligible",
+      blockedByPolicy: true,
+    });
+
+    await engine.maintain({
+      sessionId,
+      sessionFile: createSessionFilePath("maintain-policy-blocked-no-backoff"),
+      runtimeContext: {
+        allowDeferredCompactionExecution: true,
+        tokenBudget: 4_096,
+        currentTokenCount: 3_500,
+      },
+    });
+
+    const maintenance = await engine
+      .getCompactionMaintenanceStore()
+      .getConversationCompactionMaintenance(conversation.conversationId);
+    expect(maintenance?.pending).toBe(true);
+    expect(maintenance?.lastFailureSummary).toBeNull();
+    expect(maintenance?.retryAttempts).toBe(0);
+    expect(maintenance?.nextAttemptAfter).toBeNull();
+  });
+
   it("maintain() uses the stricter current token budget for deferred threshold debt", async () => {
     const engine = createEngine();
     const privateEngine = engine as unknown as {
diff --git a/test/spend-guard-policy.test.ts b/test/spend-guard-policy.test.ts
index 1c1ad27..77a4457 100644
--- a/test/spend-guard-policy.test.ts
+++ b/test/spend-guard-policy.test.ts
@@ -231,6 +231,39 @@ describe("summary spend guard policy", () => {
     expect(engine.getCompactionGuards().getSummarySpendBackoffUntil(spendScopeKey(engine, sessionId))).not.toBeNull();
   });

+  it("does not open spend backoff when eligibility policy blocks the next sweep", async () => {
+    const { engine, log } = createEngine();
+    const sessionId = "spend-policy-blocked-no-backoff";
+    await seedConversation(engine, sessionId);
+
+    const internals = privateEngine(engine);
+    vi.spyOn(internals.compaction, "evaluate").mockResolvedValue(thresholdEvaluation);
+    vi.spyOn(internals.compaction, "compact")
+      .mockResolvedValueOnce({ actionTaken: true, tokensBefore: 5_000, tokensAfter: 4_500 })
+      .mockResolvedValueOnce({
+        actionTaken: false,
+        tokensBefore: 4_500,
+        tokensAfter: 4_500,
+        blockedReason: "no-eligible-candidates",
+      });
+
+    const result = await engine.compact({
+      sessionId,
+      sessionFile: createSessionFile("spend-policy-blocked"),
+      tokenBudget: 4_096,
+      currentTokenCount: 5_000,
+      compactionTarget: "threshold",
+    });
+
+    expect(result.ok).toBe(false);
+    expect(result.blockedByPolicy).toBe(true);
+    expect(result.reason).toBe("compaction deferred until more context is eligible");
+    expect(engine.getCompactionGuards().getSummarySpendBackoffUntil(spendScopeKey(engine, sessionId))).toBeNull();
+    expect(log.info).toHaveBeenCalledWith(
+      expect.stringContaining("reason=eligibility_policy"),
+    );
+  });
+
   it("chains threshold sweeps until the target is reached", async () => {
     const { engine } = createEngine();
     const sessionId = "spend-chained-success";
